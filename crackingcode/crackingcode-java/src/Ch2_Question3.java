public class Ch2_Question3 {

    //Algorithm to delete node in middle of linked list
    //Given: access to the middle node

    //if(node == head || node == tail || node == null)
    //    return false;

    //Node prev = node.prev;
    //Node next = node.next;
    //prev.next = next;
    //next.prev = prev;
    //return true;

}
